fastapi
python-multipart
uvicorn[standard]
python-dotenv
pydantic
pandas
numpy
# Match sklearn version used when training/saving .pkl models (avoids unpickle warnings)
scikit-learn>=1.6.1,<1.7.0
joblib
pillow
# Plant disease image classifier — ONNX Runtime (CPU) is used instead of PyTorch.
# onnxruntime uses ~80MB RAM vs PyTorch's ~350MB, keeping us inside Render's 512MB free limit.
onnxruntime
requests
