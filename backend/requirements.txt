fastapi
python-multipart
uvicorn[standard]
python-dotenv
pydantic
pandas
numpy
# Match sklearn version used when training/saving .pkl models (avoids unpickle warnings)
scikit-learn>=1.6.1,<1.7.0
joblib
pillow
# Plant disease classifier uses public PlantVillage MobileNetV2 (PyTorch) checkpoint.
# Install CPU wheels via: pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
torch>=2.1
torchvision>=0.16
# huggingface_hub is used only for auto-downloading model weights on first boot.
# transformers / safetensors are NOT installed (cereal ViT not deployed on free tier).
huggingface_hub>=0.23
requests
