# Full backend URL (no trailing slash). Empty = same origin; Vite dev proxies /api → localhost:8000.
# VITE_API_BASE_URL=http://localhost:8000

# false = call the real FastAPI backend (Swabhiman ML agents). true = browser-only mock.
VITE_USE_MOCK_AI=false
