# Optional: full backend URL (no trailing slash). If empty, requests use /api (Vite proxy → localhost:8000).
# VITE_API_BASE_URL=http://localhost:8000

# Set to true for demo mode — no FastAPI required; uses built-in structured replies.
VITE_USE_MOCK_AI=true
